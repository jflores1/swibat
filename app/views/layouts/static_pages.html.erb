<!DOCTYPE html>
<html>
<head>
  <%= render 'layouts/head_content' %>
</head>
<body class="patterned">
    <div id="main" role="main">
        <div class="content">
          <div class="row-fluid">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= render 'partials/partial_new_lead_modal' %>
            </div><!--end of .span12 -->
          </div><!--end of .row-fluid -->

          <%= yield %>

          <div class="row-fluid">
            <div class="span12">
              <%= render 'layouts/footer' %>
              <%= debug(params) if Rails.env.development? %>
            </div>
          </div>


        </div><!--end of .content> -->
    </div><!--end of .main> -->
</body>
</html>
