<h1><%= @institution.name %> Evaluation Template</h1>
<p>Edit as often as you like. We've taken the liberty of prepopulating it with some commonly used evaluation criteria. Keep all of it. Or none of it. Your call. Click "Publish" it when you're ready to make it official and share it with the school!</p>

<div class="eval-criteria">
	<div class="course-organizer">
		<span>Last updated <%= time_ago_in_words(@template.updated_at) %> ago</span>

		<div class="box-row" id="evaluation-domains">
			<% @template.evaluation_domains.each do |domain| %>
				<%= render :partial => "evaluation_domains/evaluation_domain", locals: {domain: domain} %>
			<% end %>
		</div>
		<p><%= link_to "Add Domain", new_institution_evaluation_template_evaluation_domain_path(@institution, @institution.evaluation_template), class:"btn btn-monkey", remote: true %></p>
		<div id="domain-form" style="display: none;"></div>

		<div class="btn-group">
			<%= link_to "Publish", "#", class:"btn btn-monkey" %>
		</div>
	</div>
</div>

