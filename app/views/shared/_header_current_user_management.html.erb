<% if can? :edit, current_user.institution %>
	<li class="dropdown">
		<a href="#" class="dropdown-toggle" data-toggle="dropdown">manage <b class="caret gray-caret"></b></a>
		<ul class="dropdown-menu">
			<% if current_user.institution.present? %>
		  	<li><%= link_to "School Info", edit_institution_path(current_user.institution) %></li>
		  	
		  	<li><%= link_to "Faculty", faculty_institution_path(current_user.institution) %></li>
		  	<li><%= link_to "Evaluations", institution_evaluations_path(current_user.institution) %></li>
		  <% end %>
		</ul>
	</li>
<% end %>