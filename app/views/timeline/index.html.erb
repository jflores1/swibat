<h1>Recent activity</h1>

<ul id="activity-list">
<% @activities.each do |activity| %>
	<% next if !activity.recipient %>
	<li class="activity-item">
  	<%= render_activity(activity) %>
  </li>
<% end %>
</ul>